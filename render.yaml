databases:
  - name: nest-postgres-fallback
    databaseName: nest
    user: nest
    plan: free

services:
  - type: web
    name: nest-api-fallback
    runtime: node
    rootDir: .
    plan: free
    autoDeploy: false
    buildCommand: npm install
    startCommand: npx tsx apps/api/src/server.ts
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_PORT
        value: 10000
      - key: POSTGRES_DB
        fromDatabase:
          name: nest-postgres-fallback
          property: database
      - key: POSTGRES_USER
        fromDatabase:
          name: nest-postgres-fallback
          property: user
      - key: POSTGRES_PASSWORD
        fromDatabase:
          name: nest-postgres-fallback
          property: password
      - key: POSTGRES_HOST
        fromDatabase:
          name: nest-postgres-fallback
          property: host
      - key: POSTGRES_PORT
        fromDatabase:
          name: nest-postgres-fallback
          property: port
      - key: CORS_ORIGINS
        value: https://nest-azure-zeta.vercel.app
      - key: RUNTIME_ORIGIN
        value: render
      - key: BACKEND_ROLE
        value: fallback
